tracks:
  convert:
    actions:
    - bloom-export-upstream :{vcs_local_uri} :{vcs_type} --tag :{release_tag} --display-uri
      :{vcs_uri} --name :{name} --output-dir :{archive_dir_path}
    - git-bloom-import-upstream :{archive_path} :{patches} --release-version :{version}
      --replace
    - git-bloom-generate -y rosrelease :{ros_distro} --source upstream -i :{release_inc}
    - git-bloom-generate -y rosdebian --prefix release/:{ros_distro} :{ros_distro}
      -i :{release_inc}
    devel_branch: null
    name: upstream
    patches: null
    release_inc: -1
    release_repo_url: null
    release_tag: :{version}
    ros_distro: groovy
    vcs_type: svn
    vcs_uri: svn://scm.gforge.inria.fr/svn/visp
    version: :{auto}
  groovy:
    actions:
    - bloom-export-upstream :{vcs_local_uri} :{vcs_type} --tag :{release_tag} --display-uri
      :{vcs_uri} --name :{name} --output-dir :{archive_dir_path}
    - git-bloom-import-upstream :{archive_path} :{patches} --release-version :{version}
      --replace
    - git-bloom-generate -y rosrelease :{ros_distro} --source upstream -i :{release_inc}
    - git-bloom-generate -y rosdebian --prefix release/:{ros_distro} :{ros_distro}
      -i :{release_inc}
    devel_branch: null
    name: upstream
    patches: groovy
    release_inc: -1
    release_repo_url: null
    release_tag: null
    ros_distro: groovy
    vcs_type: svn
    vcs_uri: svn://scm.gforge.inria.fr/svnroot/visp/tags/ViSP_2_8_0
    version: :{ask}
  hydro:
    actions:
    - bloom-export-upstream :{vcs_local_uri} :{vcs_type} --tag :{release_tag} --display-uri
      :{vcs_uri} --name :{name} --output-dir :{archive_dir_path}
    - git-bloom-import-upstream :{archive_path} :{patches} --release-version :{version}
      --replace
    - git-bloom-generate -y rosrelease :{ros_distro} --source upstream -i :{release_inc}
    - git-bloom-generate -y rosdebian --prefix release/:{ros_distro} :{ros_distro}
      -i :{release_inc}
    devel_branch: null
    last_version: 2.8.0
    name: upstream
    patches: hydro
    release_inc: '5'
    release_repo_url: null
    release_tag: null
    ros_distro: hydro
    vcs_type: svn
    vcs_uri: svn://scm.gforge.inria.fr/svnroot/visp/tags/ViSP_2_8_0
    version: :{ask}
